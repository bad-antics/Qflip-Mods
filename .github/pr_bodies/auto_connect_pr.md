Adds a background auto-connect worker for Flipper (serial auto-detect) and Pineapple (auth retry), a `/status/devices` endpoint, detailed serial logging (VID/PID), unit tests for the new endpoints, UI improvements including a typewriter effect for outputs, and tests.

This PR is a DRAFT for review and CI.
